import { HandleProfilePage, makeHandleMetadata } from "@/components/HandleProfilePage";

export const generateMetadata = makeHandleMetadata("ayur");
export default HandleProfilePage({ namespace: "ayur" });
