# EAS Build — files to exclude from the upload archive.
# Native dirs are regenerated by `expo prebuild` on EAS.
ios/
android/

# Expo cache
.expo/

# Turbo cache
.turbo/

# Build artifacts
dist/
web-build/

# Heavy files not needed for the build
*.orig.*
.DS_Store
*.log
